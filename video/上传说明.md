# 上传到 GitHub 说明

## 前提条件

1. **安装 Git for Windows**
   - 下载地址: https://git-scm.com/download/win
   - 安装后重启终端

2. **配置 Git（首次使用）**
   ```bash
   git config --global user.name "你的名字"
   git config --global user.email "你的邮箱"
   ```

3. **GitHub 认证**
   - 如果使用 HTTPS，需要 Personal Access Token（不是密码）
   - 生成 Token: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - 或者使用 SSH 密钥

## 方法一：使用脚本（推荐）

直接运行 PowerShell 脚本：
```powershell
powershell -ExecutionPolicy Bypass -File upload_to_github.ps1
```

## 方法二：手动执行命令

如果脚本无法运行，可以手动执行以下命令：

```bash
# 1. 初始化 Git 仓库（如果还没有）
git init

# 2. 设置远程仓库
git remote add origin https://github.com/xujl1999/data-management.git
# 或者如果已存在，更新地址：
git remote set-url origin https://github.com/xujl1999/data-management.git

# 3. 从 Git 中移除敏感文件（保留本地文件）
git rm --cached bili_cookies.json -f

# 4. 添加所有文件
git add .

# 5. 提交更改
git commit -m "更新 mini-bilibili 项目"

# 6. 推送到 GitHub
git branch -M main  # 确保分支名为 main
git push -u origin main
```

## 注意事项

- ✅ `bili_cookies.json` 已被 `.gitignore` 忽略，不会上传
- ⚠️ 如果文件已经被 Git 跟踪，需要先用 `git rm --cached` 移除
- 🔐 推送时需要 GitHub 用户名和 Personal Access Token

## 如果遇到问题

1. **分支名问题**: 如果默认分支是 `master` 而不是 `main`，使用：
   ```bash
   git push -u origin master
   ```

2. **认证失败**: 使用 Personal Access Token 而不是密码

3. **远程仓库已存在内容**: 可能需要先拉取：
   ```bash
   git pull origin main --allow-unrelated-histories
   ```

